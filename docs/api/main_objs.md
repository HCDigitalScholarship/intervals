# main_objs

Import functions come first, since they're the usual starting point,
followed by `ImportedPiece` and its supporting functions; `CorpusBase`
follows since it builds on `ImportedPiece`.

::: crim_intervals.main_objs
    options:
      show_root_heading: false
      members:
        - importScore
        - Crimport
        - ImportedPiece
        - joiner
        - clean_melody_new
        - CorpusBase
